.navbar.navbar-inverse.navbar-fixed-top
    .container
        .navbar-header
            button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar")
                span .sr-only Toggle navigation
                span .icon-bar
                span .icon-bar
                span .icon-bar
            a.navbar-brand(href="/#") RAASTA
        #navbar.navbar-collapse.collapse
            ul.nav.navbar-nav
                li: a(href="/#demo") Demo
                li: a(href="/#quick") Quick Start
                li: a(href="/#usage") Usage
                li: a(href="/#about") About
                li: a(href="/#contact") Contact
            form.navbar-form.navbar-right
                .form-group: input#registerEmailField.form-control(type="text" placeholder="Email")
                script.
                    var submitRegistration = function() {
                        event.preventDefault();
                        var email = document.getElementById("registerEmailField").value;
                        raasta.register({
                            email:email,
                            success:function(response) {
                                console.log(response);
                                document.location = '/welcome';
                            }
                        });
                    };
                | &nbsp;
                button.btn.btn-success(onclick="submitRegistration(); return false;") Register